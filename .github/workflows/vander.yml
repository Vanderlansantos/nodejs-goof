name: vanderlan-santos
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  info-teste:
    runs-on: ubuntu-latest
    steps:
      - run: uname -a 
      - run: id
      - run: env
